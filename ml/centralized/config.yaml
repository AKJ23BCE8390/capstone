dataset:
  name: "pneumoniamnist"
  image_size: 64
  channels: 1
  num_classes: 2
  batch_size: 32
  raw_dir: "./data/raw"

model:
  architecture: "resnet18"
  pretrained: true

training:
  epochs: 15
  learning_rate: 0.001
  weight_decay: 0.0001
  seed: 42